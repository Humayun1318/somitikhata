
import { reportsSummary } from '@/lib/mock-data';

export default function AdminReportsPage() {
  return (
   <div className="space-y-6"><header className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-app-primary">Insights</p><h1 className="mt-1 text-2xl font-semibold text-app-text">Reports</h1><p className="mt-1 text-sm text-app-text-muted">A clear snapshot of cooperative performance.</p></div><button className="w-fit rounded-lg bg-app-primary px-4 py-2.5 text-sm font-semibold text-white">Export PDF</button></header><section className="grid gap-4 md:grid-cols-3">{reportsSummary.map((item) => <article key={item.label} className="rounded-2xl border border-app-border bg-app-surface p-5"><p className="text-sm text-app-text-muted">{item.label}</p><p className="mt-3 text-2xl font-bold text-app-text">{item.value}</p><div className="mt-5 h-2 rounded-full bg-app-surface-muted"><div className="h-2 w-3/4 rounded-full bg-app-primary" /></div><p className="mt-2 text-xs text-app-text-muted">Compared with last month</p></article>)}</section><section className="rounded-2xl border border-app-border bg-app-surface p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-app-text">Collection trend</h2><p className="mt-1 text-sm text-app-text-muted">Last six months</p></div><span className="text-sm font-semibold text-app-primary">+14.8%</span></div><div className="mt-8 flex h-40 items-end gap-3">{[42, 55, 48, 68, 76, 92].map((height, index) => <div key={index} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-lg bg-app-primary/80" style={{ height: `${height}%` }} /><span className="text-xs text-app-text-muted">{['Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep'][index]}</span></div>)}</div></section></div>
  );
}
